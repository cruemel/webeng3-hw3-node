# Required metadata
sonar.projectKey=localhost:tmc
sonar.projectName=truespeed
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=src

# Language
sonar.language=js

# Encoding of sources files
sonar.sourceEncoding=UTF-8
